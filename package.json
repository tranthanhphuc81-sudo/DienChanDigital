{
  "name": "dienchan-firebase-sync",
  "version": "1.0.0",
  "description": "Cloudinary → Firestore sync helper for DienChan Firebase UI",
  "main": "cloudinary-firestore-sync.js",
  "scripts": {
    "sync": "node cloudinary-firestore-sync.js"
  },
  "dependencies": {
    "firebase-admin": "^11.0.0"
  }
}
